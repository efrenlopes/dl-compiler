programa area_circulo inicio
  inteiro raio;
  booleano positivo;
  raio = 5;
  positivo = (raio > 1 | raio == 1);
  se (positivo == verdade) inicio
    inteiro area;
    area = 3 * raio * raio;
    escreva(area);
  fim;
fim.